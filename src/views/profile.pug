extends layout

block content
    -
        function profileHistoryTime(message) {
            const rawDate = message.timestamp || message.createdAt
            const date = new Date(rawDate)
            if (Number.isNaN(date.getTime())) {
                return String(message.createdAt || '').replace(/\s+[A-Z]{2,5}$/, '')
            }
            const today = new Date()
            const yesterday = new Date()
            yesterday.setDate(today.getDate() - 1)
            const sameDay = (a, b) => a.getFullYear() === b.getFullYear() && a.getMonth() === b.getMonth() && a.getDate() === b.getDate()
            const dayLabel = sameDay(date, today)
                ? 'Today'
                : sameDay(date, yesterday)
                    ? 'Yesterday'
                    : new Intl.DateTimeFormat('en-GB', { day: '2-digit', month: '2-digit', year: 'numeric' }).format(date)
            const time = new Intl.DateTimeFormat('en-GB', {
                hour: '2-digit',
                minute: '2-digit',
                second: '2-digit',
                hour12: false,
            }).format(date)
            return dayLabel + ', ' + time
        }

    .page-hero
        h2.page-title= title
        p.page-subtitle Command activity and account overview.

    .profile-panel
        .profile-avatar
            div
                if avatarUrl
                    img(src=avatarUrl, alt="No avatar")
                else
                    span #{(displayName || '?').charAt(0).toUpperCase()}
        .profile-body
            h3.profile-name #{displayName}
            .profile-stat-grid
                .stat-card.profile-stat-blue
                    .stat-label All-time rank
                    .stat-value #{stats.allTimePosition ? '#' + stats.allTimePosition : 'n/a'}
                .stat-card.profile-stat-blue
                    .stat-label All-time commands
                    .stat-value #{stats.total}

    .content-panel.profile-stat-panel
        .panel-header
            .panel-title-group
                span.panel-icon(aria-hidden="true")
                    svg(xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round")
                        rect(x="3" y="4" width="18" height="18" rx="2" ry="2")
                        line(x1="16" y1="2" x2="16" y2="6")
                        line(x1="8" y1="2" x2="8" y2="6")
                        line(x1="3" y1="10" x2="21" y2="10")
                h3.panel-title This Month
        .stat-grid.profile-stat-grid
            .stat-card.profile-stat-green
                .stat-label Rank
                .stat-value #{stats.currentMonthPosition ? '#' + stats.currentMonthPosition : 'n/a'}
            .stat-card.profile-stat-green
                .stat-label Commands
                .stat-value #{stats.currentMonth}
            .stat-card.profile-stat-yellow
                .stat-label Last 24 hours
                .stat-value #{stats.lastDay}


    if isOwn && history
        .content-panel.mt-4.profile-history-panel
            .panel-header
                .panel-title-group
                    span.panel-icon(aria-hidden="true")
                        svg(xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round")
                            path(d="M4 7h16")
                            path(d="M4 12h12")
                            path(d="M4 17h8")
                    h3.panel-title Last 24 Hours Commands
            .data-table-wrap.data-table-wrap-compact
                table.data-table
                    thead
                        tr
                            th(scope='col') Command
                            th(scope='col') Time
                    tbody
                        each message in history
                            tr
                                td.break-all #{message.content}
                                td.data-muted #{profileHistoryTime(message)}


    p &nbsp;
    p &nbsp;
